# Port (default 4000)
PORT=4000

# OpenAI API key – if set, the server will use AI to generate content; otherwise returns mock data
OPENAI_API_KEY=

# Optional: model name (default gpt-4o-mini)
# OPENAI_MODEL=gpt-4o-mini
